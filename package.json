{
  "name": "play-and-star",
  "version": "1.0.0",
  "description": "",
  "main": "index.html",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "serve": "live-server src",
    "watch:sass": "sass ./src/assets/scss/styles.scss ./src/assets/css/styles.css --watch",
    "start": "concurrently  \" npm run watch:sass \" \" npm run serve \" " 
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.1",
    "live-server": "^1.2.2",
    "node-sass": "^9.0.0"
  }
}
